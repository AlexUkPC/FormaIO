<td>
  <%= link_to user.email, user %>
</td>
<% if can? :manage, User %>
  <td>
    <%= form_for(user) do |f| %>
      <%= f.select(:role, User.roles.keys.map {|role| [role.titleize,role]}) %>
      <%= f.submit 'Change Role' %>
    <% end %>
  </td>
  <td>
    <%= link_to("Delete user", user_path(user), :data => { :confirm => "Are you sure?" }, :method => :delete) unless user == current_user %>
  </td>
<% end %>