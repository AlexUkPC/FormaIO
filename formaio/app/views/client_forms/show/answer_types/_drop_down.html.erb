<% if @answer_form %>
  <%= collection_select( question.id,@answer_form.client_form.properties[section.id.to_s][question.id.to_s], question.posible_answer, :id, :answer, selected: @answer_form.client_form.properties[section.id.to_s][question.id.to_s]) %>
<% else %>
  <%= collection_select( question.id,@client_form.properties[section.id.to_s][question.id.to_s], question.posible_answer, :id, :answer, selected: @client_form.properties[section.id.to_s][question.id.to_s]) %>
<% end %>