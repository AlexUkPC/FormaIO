<div class="field">
  <%= f.label question.id, question.question %>
  <%= f.collection_radio_buttons( question.id, question.posible_answer, :id, :answer, checked: @client_form.properties[question.id.to_s] ) do |b| %>
    <% b.label { b.radio_button + b.text } %>
  <% end %>
</div>