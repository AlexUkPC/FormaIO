<div class="field">
  <%= f.label question.id, question.question %>
  <%= f.collection_select( question.id, question.posible_answer, :id, :answer, selected: @client_form.properties[section.id.to_s][question.id.to_s]) %>
</div>